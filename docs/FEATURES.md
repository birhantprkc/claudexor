# Feature Status Ledger

This file tracks every user-facing feature that is NOT currently in a `solid`
state. It exists so an agent touching a feature inherits the known state
instead of rediscovering it. Rule (see `CONTRIBUTING.md`): when a change
alters a feature listed here, update or delete its row in the same commit. A
feature that becomes solid loses its row — this ledger shrinks toward empty;
growth is a regression signal.

Statuses: `broken` > `dead` (wired to nothing) > `half-baked` > `suspicious` >
`works-with-caveats`. `Planned` names the stabilization-program phase that
owns the fix (`backlog` = not yet scheduled). Evidence is file:line at the
time of the audit; lines drift with edits — verify before relying on them.

Rows: **11** (works: 1, works-with-caveats: 10)

| Area | Feature | Status | What is wrong / caveat | Evidence | Planned |
|---|---|---|---|---|---|
| engine/web-evidence | Codex web-retrieval verification (QA-042) | works-with-caveats | Codex `exec --json` `web_search`/`open_page` items carry NO typed fetch outcome — a `completed` item can hide a 502 Bad Gateway. Claudexor therefore stamps codex web results `web_retrieval: dispatched` (the call completed) and records `web.verification: dispatched`, NOT `verified`: a surface must render "Web reached", never "Web verified", for a codex-only web route. Web is optional, so this strength is observational rather than a terminal gate. Claude's WebSearch/WebFetch expose typed content and stamp `verified`. Closing the codex gap needs an upstream typed result (the July-2026 0.145 "structured standalone web search results" changelog is a lead, unverified on `exec --json`). Do NOT prose-parse the answer to recover the outcome (INV-049) | packages/harness-codex/src/parse.ts (web_search `web_retrieval: dispatched`); packages/orchestrator/src/attemptTelemetry.ts (`bumpWebVerification`, `web.verification`); packages/harness-codex/fixtures/basic-run.jsonl (completed web_search carries only status) | Ф3 (macOS label) / upstream |
| engine/work-report | D-16 WorkReport / work_state axis (v3.1 Ф1) | works-with-caveats | D-16a–d ship the schema contract, the transport envelope, the unified finalizer, the work_state veto, the outcome-aware exit, the adapter context signals, and the one-shot continuation. The transport is now ACTIVE on all three tiers: `constrained` (codex `--output-schema`, claude `--json-schema` with a caller schema), claude `side_tool` (a `{work_report}`-only StructuredOutput tool while the markdown final stays the deliverable), and cursor `validated` (the complete markdown final followed by an instructed fenced `{work_report}` metadata block — a missing/malformed footer is a typed `work_report_contract` failure; historical fence-only `{work_report, output}` replies remain readable). Claude maps the fixture-proven context signals (`terminal_reason` prompt_too_long/rapid_refill_breaker → capacity_exhausted, `compact_boundary` → compaction, the typed `rate_limit_event`). Remaining caveats: codex context-exhaustion stays honestly GENERIC — codex 0.144.1 exec surfaces oversized input only as a stderr JSON-RPC error (`input_error_code: input_too_large`), never a typed stream frame, so no context event is mapped until upstream surfaces one; claude's `--json-schema` × interactive stream-json lane still discloses the transport `unsupported`; and the continuation is eligible only for claude `repeated_refill` (prompt_too_long is treated as an irreducible packet). Codex-specific display note (openai/codex #19816): `--output-schema` constrains INTERMEDIATE agent messages too, not just the final one, so a mid-run narration arrives as the raw `{work_report, output}` envelope — the codex adapter now typed-detects that shape (exact `work_report`+`output` keys, `work_report` matching the WorkReport schema) and unwraps it to its `output` for the visible stream (suppressing a non-string `output`), so raw envelope JSON never reaches the progress bubble; the FINAL message likewise unwraps its DISPLAY text to `output` (so the answer bubble and the reducer's twin-removal see the same unwrapped text the intermediate copy carried — the QA-009 twin fix), while the RAW `{work_report, output}` envelope rides `payload.work_report_envelope`; the orchestrator reads machine truth via `AnswerAssembly.machineText()`, and a non-string/non-envelope final keeps its raw text with `machineText()` falling back to it. Claude (`side_tool`) and cursor (instructed fence on the final answer only) never carry the envelope in the intermediate text stream, so this is codex-only | packages/harness-claude/src/parse.ts (context + rate_limit + side_tool); packages/harness-codex/src/parse.ts (`detectEnvelopeOutput` intermediate unwrap/suppress; `CodexParseState.envelopeActive`); packages/harness-codex/fixtures/structured-envelope-intermediate.jsonl; packages/orchestrator/src/attemptFinalize.ts (`resolveWorkReportEnvelope` activates all tiers); packages/orchestrator/src/continuation.ts; packages/harness-codex/fixtures/context/exec-oversized-input.stderr.txt (documented upstream gap) | Ф1 (dogfood) |
| macos/updater | Engine-runtime auto-install (SHIPPED in 3.1.0 — D-2) | works | One-click in-app update: signed runtime manifest (offline Ed25519 authority, pinned key, fail-closed verify), download → sha256-verify → unpack → busy-gate (refuses while runs/jobs are active) → identity-stop the daemon → atomic current.json pointer swap under flock → relaunch → handshake-verify → rollback to last-known-good on ANY post-swap failure; bundled runtime stays the final fallback. The same single closure now materializes internal package links into a regular-file/directory-only archive for exact-pinned host embedding, without adding another payload or trust root. Exact-candidate-artifact promotion in the release workflow (candidate_run_id + gh attestation verify + byte-identity). `claudexor release check` verifies the same signed manifest fail-closed | apps/macos/.../RuntimeInstallCoordinator.swift, RuntimeUpdater.swift, RuntimeUpdate.swift; scripts/build-runtime-closure.mjs; scripts/lib/runtime-manifest-contract.mjs; packages/util/src/runtime-manifest.ts | — |
| engine/convergence | Typed run-failure refusal in the `until-clean` lane | works-with-caveats | A typed pre-spawn refusal (`subscription_window_exhausted` with its structural `resetsAt`) is written typed in EVERY strategy lane: quota admission runs during ROUTING (`preflightProfile` inside `resolveCandidateAdapters`), and the race, convergence, Plan, and read-only routing catches all call `writeRoutingFailureTerminal`, which preserves the declared `code` and `resetsAt` in `failure.yaml` — a scheduler driving `--until-clean` reads the refusal typed, never from prose. Remaining caveat (the only asymmetry): a MID-ATTEMPT window exhaustion — one that surfaces after routing, inside a spawned attempt — is promoted as a typed `DeclaredFailure` only by the race lane's unanimity terminal; `runConvergence`'s per-attempt catch still reduces that late shape to a message string | packages/orchestrator/src/routing-failure.ts (`routingFailureClassification` + `writeRoutingFailureTerminal` preserve code+resetsAt; the header comment states the routing-only placement); packages/orchestrator/src/orchestrator.ts (runConvergence routing catch calls `writeRoutingFailureTerminal`; race per-slot catch records `declaredFailure`, the convergence per-attempt catch does not); packages/orchestrator/src/candidateEvidence.ts `unanimousDeclaredFailure` (race terminal promotion) | backlog |
| engine/profiles | Profile-policy `limit_action: ask` interactive UX (2.1) | works-with-caveats | The engine records the typed `route.profile.headroom_exceeded` breach and PROCEEDS on the selected profile — no surface actually asks the user yet (the router consumer shipped first, sol #28) | packages/orchestrator/src/credential-profiles.ts preflightCredentialProfile (`ask` falls through to proceed) | backlog |
| engine/profiles | Named Cursor accounts via the vendor file store (v3.3.7) | works-with-caveats | Cursor `config_dir_login` profiles select the CLI's vendor-supported file credential store (`AGENT_CLI_CREDENTIAL_STORE=file`) inside a Claudexor-owned profile HOME; the default login keeps the OS Keychain untouched, and `CURSOR_CONFIG_DIR`/`CURSOR_DATA_DIR` stay in the run's lane HOME so sessions never mix. Store isolation is live-proven WITHOUT a login (two scratch-HOME `cursor-agent status` probes land all file-store state in the scratch store and leave the real Keychain untouched — no login/OAuth write was exercised, so the evidence covers store selection, not a login write); cursor has no vendor quota source yet, so quota tracking and limit-triggered rotation do not cover cursor profiles and the auto-switch toggle excludes them; the remaining caveat is that a real second-account OAuth login + profiled run has not been executed end-to-end yet — it rides the post-publish dogfood pass | packages/harness-cursor/src/profile.ts (file-store env + strict routing); packages/cli/src/native-login.ts (profile login env); packages/harness-cursor/src/profile.test.ts; packages/cli/src/native-login.test.ts | v3.3.7 release program (post-publish valentine OAuth) |
| macos+cursor | Cursor-harness manual QA phases (v3.0.0) | works-with-caveats | The v3.0.0 release verified cursor through the automated battery + typed-refusal canaries only; the interactive manual-QA phases (live cursor plan/agent runs driven from the app) were waived by owner decision for this release and ride the next dogfood pass | docs/CHECKLISTS.md release protocol (manual-QA waiver rule); real-harness-battery cursor rows | backlog |
| acp/interactions | ACP free-text mid-run questions (v3.0.0) | works-with-caveats | ACP's permission mechanism is choice-only, so an option-less (free-text) mid-run question cannot be answered inside the editor; the surface never silently skips it — it is disclosed as turn text naming the remedy and the run stays paused until answered via `claudexor follow <run>` or `POST /v2/runs/:id/interactions/:id/answer`. Choice and multi-select questions ARE answered inline (multi-select via per-option include/skip rounds) | packages/acp-server/src/index.ts requestAnswers; packages/acp-server/src/acp.test.ts (option-less turn-text + multi-select) | backlog |
| engine/effort | Per-model reasoning-effort ladders | works-with-caveats | Effort is now discovered live and resolved per (harness, model): codex `app-server` `model/list` reports `supportedReasoningEfforts` per model, so `ultra` reaches gpt-5.6-sol while gpt-5.4 correctly stops at `xhigh`. Claude has NO per-model source — the vendor CLI exposes only one `--effort` list for the whole binary — so `model_effort_levels` stays empty for claude and every claude model is held to the CLI-wide ladder parsed from `claude --help`. A model whose real ceiling is lower than that ladder is therefore not narrowed, and an over-strong request clamps at the CLI ceiling instead of the model's. Closing this needs an upstream per-model effort source. Both probes fall back to a recorded snapshot when they cannot answer (a probe failure never fails a run), and `scripts/model-hints-freshness.mjs` warns when live ladders disagree with the snapshot | packages/harness-codex/src/effort-probe.ts (`model/list` probe + snapshot); packages/harness-claude/src/effort-probe.ts (`--help` parse, no per-model source); packages/schema/src/effort.ts (`effortLevelsForModel`); packages/core/src/effort.ts (`resolveEffort`) | backlog |
| acp/auth | ACP Terminal Auth (experimental) | works-with-caveats | Only Codex on macOS/Linux is advertised, and only when the client opts into terminal auth. The method is proactive from `initialize`; Claudexor does not yet emit `auth_required` or implement legacy `authenticate`. Claude/Cursor stay absent until their full terminal lifecycle is provable | packages/acp-server/src/auth.ts; packages/cli/src/acp-auth.ts; packages/acp-server/src/acp.test.ts | backlog |
| harness/agy | Antigravity CLI (Gemini subscription) harness | works-with-caveats | Named Google identities are Claudexor-owned profile HOMEs — live-proven on three separate Google accounts, two of them answering at the same time and the third in a fresh sequential process, with the operator's real home and login keychain untouched (concurrent RUNS on two profiles are still unverified). Caveats in v1: (a) `agy` exposes no config-dir env var (upstream #155), so the profile HOME also holds the vendor's conversations/brain/cache — the per-lane home is unused for agy, one profile accumulates every thread's vendor state, and conversations are NOT scoped per chat thread — a lane resumes its own vendor conversation by id, but nothing isolates them; (b) no machine-readable account identity exists — the vendor prints the email only into the profile log, so an agy profile shows its display name, not its Google address; (c) quota windows differ by subscription tier (a second live account had no 5h window at all), so the parser reports the windows the account actually has instead of a fixed set; (d) the vendor is a closed signed Go binary with no source repository, so wire shapes are pinned by fixtures — recorded from live sessions except the synthesized resume stream, with the soft-deny class in (e) carrying no fixture at all — and must be re-verified when the pinned version moves; (e) `--dangerously-skip-permissions` is the only full-access lever the vendor offers, and a `SUCCESS` with an empty response (its soft-deny class, upstream #794) is surfaced as a typed error rather than an empty success; (f) Windows and Linux are best effort: the installer runs the vendor's own PowerShell script on Windows and the POSIX one elsewhere, but the account-isolation mechanism has only been live-proven on macOS, and `claudexor doctor` says so on every other platform instead of implying a proof it does not have | packages/harness-agy/src/profile.ts (profile HOME routing + token file); packages/harness-agy/fixtures/manifest.yaml (recorded provenance); packages/cli/src/agy-quota-source.ts (`/quota` per profile); packages/cli/src/harness-installer.ts (script-installer row) | backlog |

New rows are added the moment a non-solid feature ships (see the rule above).
Deliberate design boundaries that used to live here as "caveats" are
documented in the "Design constraints" sections of `docs/ARCHITECTURE.md`
(engine) and `docs/INTEGRATIONS.md` (host/external surfaces).

# @claudexor/harness-agy

## 3.4.1

Initial adapter: manifest/discover/doctor, stream-json parser pinned by
recorded 1.1.13 fixtures, named-profile routing (config_dir_login HOME with
the vendor's file-based token), quota-free profile liveness probe
(`-p "/model"`), and `--conversation` session resume.

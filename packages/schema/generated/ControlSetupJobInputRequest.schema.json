{
  "$ref": "#/definitions/ControlSetupJobInputRequest",
  "definitions": {
    "ControlSetupJobInputRequest": {
      "type": "object",
      "properties": {
        "value": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1024,
          "description": "The user's one-time sign-in input (e.g. the pasted OAuth code); never persisted."
        }
      },
      "required": [
        "value"
      ],
      "additionalProperties": false,
      "description": "One-shot sign-in input delivered to a waiting login job's vendor CLI."
    }
  },
  "$schema": "http://json-schema.org/draft-07/schema#"
}
